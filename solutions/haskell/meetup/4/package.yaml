name: meetup
version: 1.1.0.5

dependencies:
  - base
  - time

library:
  exposed-modules: Meetup
  source-dirs: src
  ghc-options: -Wall
  dependencies:
    - time

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - meetup
      - hspec
